machine:
  environment:
    USER_NAME: ChalkPE
    USER_EMAIL: chalk@chalk.pe
  ruby:
    version: 2.4.1

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build

deployment:
  production:
    branch: chalk
    commands:
      - ./bin/deploy
